<Window x:Class="Dental.AddTreatment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dental"
        mc:Ignorable="d"
        Title="Добавить лечение" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="14*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="17*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Дата" FontSize="22" HorizontalContentAlignment="Right"/>
        <Label Content="Описание" FontSize="22" HorizontalContentAlignment="Right" Grid.Row="1"/>
        <TextBox Grid.Column="1" Margin="2,2" Name="Date"/>
        <TextBox Grid.Column="1" Grid.Row="1" Margin="2,2" Name="Descr"/>
        <Button Grid.Row="2" Grid.ColumnSpan="2" Margin="5,10" Click="Button_Click">
            Готово
        </Button>
    </Grid>
</Window>
